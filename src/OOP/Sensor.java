package OOP;

public abstract class Sensor {

	public Sensor() {
	} // we can create constructor

	abstract void frontSensor();// abstract method does not have method body. we cant create object. an object
								// is the replica of the class.

	void backSensor() {
	}
}
